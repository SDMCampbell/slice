
  # Homepage for Content Hub

  This is a code bundle for Homepage for Content Hub. The original project is available at https://www.figma.com/design/jeCBfVGWKgGZxws3L0mfcs/Homepage-for-Content-Hub.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  